/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let stack =[];
    let bracketMap ={')': '(',']':'[','}':'{'};
    for(let char of s)
};

module.exports = { isValid };


